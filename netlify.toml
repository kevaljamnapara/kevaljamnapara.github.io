[build]
  command = "echo 'Static site - no build needed'"
  publish = "."

[context.production]
  environment = { NODE_ENV = "production" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
